# 로컬 개발용 (SQLite) — 그대로 .env 로 복사해서 사용
DATABASE_URL=sqlite:///./receipt.db

# 배포(Vercel) 환경에서는 Postgres URL 사용
# 예) DATABASE_URL=postgresql+psycopg://user:password@host:5432/dbname

# Upstage Information Extraction API 키
# 발급: https://console.upstage.ai/  (API Keys 메뉴)
UPSTAGE_API_KEY=
